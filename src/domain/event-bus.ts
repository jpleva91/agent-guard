// Domain EventBus — re-exports from core/event-bus.ts
// Mirrors domain/event-bus.js which provides a universal EventBus
// that works in both Node.js and browser environments.

export { EventBus } from '../core/event-bus.js';
